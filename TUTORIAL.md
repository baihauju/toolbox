# 🚀 零成本创建在线工具网站完整教程

> 从零开始，创建一个免费的在线工具网站，部署到 GitHub Pages

---

## 📋 目录

1. [项目概述](#1-项目概述)
2. [准备工作](#2-准备工作)
3. [创建项目](#3-创建项目)
4. [项目结构](#4-项目结构)
5. [部署到 GitHub](#5-部署到-github)
6. [启用 GitHub Pages](#6-启用-github-pages)
7. [更新网站](#7-更新网站)
8. [变现建议](#8-变现建议)
9. [常见问题](#9-常见问题)

---

## 1. 项目概述

### 1.1 项目特点

- ✅ **零成本** - 完全免费，无需服务器
- ✅ **纯前端** - 只需 HTML/CSS/JavaScript
- ✅ **隐私安全** - 所有处理在浏览器本地完成
- ✅ **易于维护** - GitHub 托管，自动部署

### 1.2 包含工具

| 类别 | 工具 |
|------|------|
| 📷 图片工具 | 图片压缩、格式转换、尺寸调整、图片裁剪 |
| 📝 文本工具 | 字数统计、文本对比、Markdown预览、JSON格式化 |
| 🔧 开发工具 | 正则测试、时间戳转换、Base64编解码、Hash计算 |
| 🎨 设计工具 | 配色方案、渐变生成器、二维码生成、Favicon生成 |

### 1.3 技术栈

- HTML5
- CSS3 (CSS Variables, Flexbox, Grid)
- 原生 JavaScript (ES6+)
- 第三方库：QRCode.js, blueimp-md5
- 部署：GitHub Pages + GitHub Actions

---

## 2. 准备工作

### 2.1 需要的账号

1. **GitHub 账号**
   - 注册地址：https://github.com
   - 用于托管代码和部署网站

### 2.2 需要的工具

1. **Git** - 版本控制
   ```bash
   # Ubuntu/Debian
   sudo apt install git
   
   # macOS
   brew install git
   
   # Windows
   # 下载安装：https://git-scm.com/download/win
   ```

2. **代码编辑器**（推荐）
   - VS Code: https://code.visualstudio.com
   - Cursor: https://cursor.sh

### 2.3 获取 GitHub Personal Access Token

1. 登录 GitHub
2. 打开：https://github.com/settings/tokens/new
3. 填写信息：
   - **Note**: `toolbox deploy`
   - **Expiration**: 90 days
   - ✅ 勾选 **repo** 权限
4. 点击 **Generate token**
5. **复制并保存 token**（只显示一次！）

---

## 3. 创建项目

### 3.1 项目目录结构

```
ai-tools-site/
├── index.html              # 主页面
├── css/
│   └── style.css           # 样式文件
├── js/
│   ├── app.js              # 主应用逻辑
│   └── tools.js            # 工具功能实现
├── assets/                 # 资源文件（图片等）
├── .github/
│   └── workflows/
│       └── deploy.yml      # GitHub Actions 部署配置
├── .gitignore              # Git 忽略文件
├── deploy.sh               # 一键部署脚本
└── README.md               # 项目说明
```

### 3.2 创建目录

```bash
mkdir -p ai-tools-site/{css,js,assets,.github/workflows}
cd ai-tools-site
```

### 3.3 核心文件说明

#### index.html - 主页面
- 包含导航栏、工具卡片、弹窗容器
- 引入样式和脚本文件
- 引入第三方库（QRCode.js, MD5.js）

#### css/style.css - 样式文件
- CSS 变量定义主题颜色
- 支持深色/浅色主题切换
- 响应式设计适配移动端

#### js/app.js - 主应用逻辑
- 主题切换功能
- 工具卡片点击事件
- 弹窗控制
- 导航平滑滚动

#### js/tools.js - 工具功能
- 各工具的 HTML 模板
- 各工具的初始化函数
- 工具功能实现代码

---

## 4. 项目结构

### 4.1 关键代码结构

```javascript
// app.js - 工具函数初始化
function initToolFunctions(toolId) {
    switch(toolId) {
        case 'word-count':
            initWordCount();
            break;
        case 'json-format':
            initJsonFormat();
            break;
        // ... 其他工具
    }
}
```

```javascript
// tools.js - 工具模板
function getToolContent(toolId) {
    const tools = {
        'word-count': `<div>...</div>`,
        'json-format': `<div>...</div>`,
        // ... 其他工具模板
    };
    return tools[toolId] || null;
}
```

### 4.2 添加新工具步骤

1. 在 `tools.js` 的 `getToolContent()` 中添加 HTML 模板
2. 在 `tools.js` 中添加初始化函数 `initXXX()`
3. 在 `app.js` 的 `initToolFunctions()` 中添加 case
4. 在 `index.html` 中添加工具卡片

---

## 5. 部署到 GitHub

### 5.1 创建 GitHub 仓库

1. 登录 GitHub
2. 点击右上角 **+** → **New repository**
3. 填写信息：
   - **Repository name**: `toolbox`
   - **Description**: 免费在线工具箱
   - 选择 **Public**
   - ⚠️ **不要勾选** "Add a README file"
4. 点击 **Create repository**

### 5.2 初始化本地 Git 仓库

```bash
cd ai-tools-site

# 初始化 Git
git init

# 重命名分支为 main
git branch -m main

# 添加所有文件
git add .

# 提交
git commit -m "🚀 Initial commit: ToolBox 在线工具箱"
```

### 5.3 连接远程仓库

```bash
# 替换 YOUR_USERNAME 为你的 GitHub 用户名
git remote add origin https://github.com/YOUR_USERNAME/toolbox.git
```

### 5.4 推送代码

```bash
git push -u origin main
```

推送时会要求输入：
- **Username**: 你的 GitHub 用户名
- **Password**: 你的 Personal Access Token（不是密码！）

---

## 6. 启用 GitHub Pages

### 6.1 配置 Pages

1. 打开你的仓库：`https://github.com/YOUR_USERNAME/toolbox`
2. 点击 **Settings**
3. 左侧菜单点击 **Pages**
4. **Build and deployment** 部分：
   - **Source** 选择：`GitHub Actions`
5. 点击右侧 **Static HTML** 的 **Configure**
6. 点击 **Commit changes** 提交

### 6.2 等待部署

1. 打开 **Actions** 标签页
2. 等待部署任务完成（✅ 绿色对勾）
3. 部署时间约 1-2 分钟

### 6.3 访问网站

部署完成后，访问：
```
https://YOUR_USERNAME.github.io/toolbox
```

---

## 7. 更新网站

### 7.1 修改代码后推送

```bash
# 添加修改的文件
git add .

# 提交（写清楚改了什么）
git commit -m "✨ 添加新功能: XXX"

# 推送
git push
```

### 7.2 处理冲突

如果推送被拒绝：

```bash
# 先拉取远程更改
git pull --rebase origin main

# 再推送
git push
```

### 7.3 自动部署

推送后，GitHub Actions 会自动重新部署，等待 1-2 分钟即可看到更新。

---

## 8. 变现建议

### 8.1 广告收入

在 `index.html` 中添加 Google AdSense：

```html
<head>
    <!-- Google AdSense -->
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-YOUR_ID" crossorigin="anonymous"></script>
</head>
```

### 8.2 打赏功能

替换打赏弹窗中的二维码占位图为真实的支付二维码。

### 8.3 付费功能

可添加需要付费解锁的高级功能：
- 批量处理
- 无水印导出
- 高级模板
- 云端存储

### 8.4 推广方式

| 平台 | 方式 |
|------|------|
| 短视频 | 抖音、B站发工具使用教程 |
| 社交媒体 | 知乎、小红书、微博分享 |
| 技术社区 | V2EX、掘金、CSDN |
| 微信群 | 相关技术群、工具群 |

---

## 9. 常见问题

### Q1: 推送时提示 "rejected"

**原因**：远程仓库有更新

**解决**：
```bash
git pull --rebase origin main
git push
```

### Q2: GitHub Pages 访问慢

**原因**：GitHub 服务器在国外

**解决方案**：
1. 部署到 Vercel（国内访问更快）
2. 部署到 Gitee Pages（纯国内）
3. 使用 CDN 加速

### Q3: 工具功能不生效

**检查**：
1. 浏览器控制台是否有错误
2. 第三方库是否正确加载
3. 函数名是否正确

### Q4: 样式显示异常

**检查**：
1. CSS 文件路径是否正确
2. 浏览器缓存（Ctrl+F5 强制刷新）
3. CSS 变量是否定义

### Q5: 如何绑定自定义域名

1. 购买域名（阿里云、腾讯云等）
2. 在域名 DNS 添加 CNAME 记录指向 `YOUR_USERNAME.github.io`
3. 在 GitHub Pages 设置中添加自定义域名
4. 等待 DNS 生效（最长 48 小时）

---

## 📚 参考资源

- [GitHub Pages 官方文档](https://docs.github.com/en/pages)
- [GitHub Actions 官方文档](https://docs.github.com/en/actions)
- [MDN Web 文档](https://developer.mozilla.org/zh-CN/)

---

## 🎉 完成！

恭喜你完成了在线工具网站的创建和部署！

如有问题，欢迎在 GitHub Issues 中反馈。

---

*文档更新时间：2026年1月7日*

